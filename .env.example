APP_NAME=PBI Lineage Backend
APP_VERSION=0.1.0
ENVIRONMENT=development
API_V1_PREFIX=/api/v1
LOG_LEVEL=INFO
XMLA_TENANT_NAME=myorg
XMLA_PROVIDER=MSOLAP
# Required for GET /auth/microsoft/sso/login; must exactly match a public-client
# redirect URI registered on the Entra app (public client flows enabled).
# MICROSOFT_SSO_REDIRECT_URI=
LINEAGE_DATABASE_PATH=data/lineage.db
LINEAGE_CACHE_TTL_SECONDS=30
LINEAGE_CACHE_MAX_ENTRIES=128
LINEAGE_SCAN_MAX_CONCURRENCY=2
SNOWFLAKE_SESSION_MAX_AGE_SECONDS=2700
SNOWFLAKE_ALLOW_EXTERNAL_BROWSER_AUTH=false
CORS_ALLOWED_ORIGINS=[]
ALLOWED_HOSTS=["*"]
FORCE_HTTPS=false
ENABLE_API_DOCS=true
AUTH_COOKIE_SECURE=false
AUTH_COOKIE_SAMESITE=lax
MAX_REQUEST_BODY_BYTES=10485760
EXPOSE_METRICS=true
# Set a strong secret in production; do not commit its value.
# LINEAGE_ADMIN_API_KEY=
